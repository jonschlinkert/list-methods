# =============================================
# API methods for <%= data.name %>
# =============================================
<% _.forEach(data, function(fn) { %>
<%- fn %>:
  - Type:        undefined
  - Default:     foo
  - Description: bar
  - Example:     baz
<% }); %>