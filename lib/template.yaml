# =============================================
# <%= data.name %> methods
# =============================================
<% _.forEach(data, function(fn) { %>
<%- fn %>:
  - Type:        String
  - Default:     foo
  - Description: bar
  - Example:     baz
<% }); %>